{
  "name": "software-sauna-code-challenge",
  "version": "1.0.0",
  "description": "A piece of code that takes a map of characters as an input and outputs the collected letters and the list of characters of the travelled path.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nina Haberl",
  "license": "ISC",
  "devDependencies": {
    "@babel/preset-typescript": "^7.22.5",
    "@types/jest": "^29.5.3",
    "jest": "^29.6.2",
    "ts-jest": "^29.1.1"
  },
  "jest": {
    "moduleFileExtensions": [
      "js",
      "jsx",
      "tsx",
      "ts"
    ],
    "rootDir": "__tests__/",
    "transform": {
      "^.+\\.(ts|tsx)?$": "ts-jest"
    }
  }
}
